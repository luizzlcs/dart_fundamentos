// ignore: todo
//TODO Modificadoress, const e final
main() {
  var nomeCompleto = 'Luiz Carlos';

  final nomeCompletoFinal = 'Luiz Carlos';
  final nomeCompletoFinal2 = nomeCompleto;

//Variaveis Finais:

// Não podem ser alteradas depois de inicializadas (Imutáveis);
// são definidas no programa em tempo de execução (Runtime);
// Utilize sem moderação.


//Variáveis Const

// Não podem ser alteredas depois de inicializadas (Imutáveis);
// São definidas no programa em tempo de COMPILAÇÃO!!!
// Não podem receber valores de outras váriaveis a não ser que essas variaveis sejam const tembém.
}
